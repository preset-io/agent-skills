# Preset CLI Skills

This directory is reserved for future Preset CLI workflow skills.

It is intentionally not an installable plugin yet:

- no `.codex-plugin/plugin.json`
- no `.claude-plugin/plugin.json`
- no `.cursor-plugin/plugin.json`
- no `skills/` directory

CLI skill design and packaging will be handled in a separate task or PR.
